









// All imports are intentionally unused to test detection
export type UserData = {
  id: number;
  name: string;
  email: string;
};

export interface Config {
  apiUrl: string;
  timeout: number;
}